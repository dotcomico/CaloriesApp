<!-- cards_container.xml -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:background="@android:color/transparent"
    android:windowSoftInputMode="adjustResize">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#00FFFFFF"
        android:padding="0dp">

        <!-- כפתור לפתיחת כרטיסיית חיפוש עצמי (כתום) -->
        <ImageView
            android:id="@+id/iv_showSelfSearchBar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="8dp"
            android:layout_marginHorizontal="10dp"
            android:layout_gravity="right"
            android:background="@drawable/circle_orang"
            android:src="@drawable/baseline_arrow_drop_up_white"
            android:visibility="gone" />

        <!-- include כרטיסיות (הוספה, עריכה, חיפוש עצמי) -->
        <include layout="@layout/card_add_new_custom_product"
            />
        <include layout="@layout/card_add_consumed_product"
           />


    </LinearLayout>
</ScrollView>
